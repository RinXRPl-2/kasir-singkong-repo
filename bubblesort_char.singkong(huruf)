var a = ["d", "a", "e", "c", "b"]
println(a)
var n = len(a)

repeat {
 var swapped = false
 var i = 1
 repeat {
  if (ord(a[i-1]) > ord(a[i])) {
   var temp = a[i-1]
   set(a, i-1, a[i])
   set(a, i, temp)
   var swapped = true
  }
  var i = i + 1
  if (i == n) {
   return null
  }
 }
 if (swapped == false) {
  return null
 }
}

println(a)
